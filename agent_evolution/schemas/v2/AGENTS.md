# AGENTS.md

本工作包待发布版本（v2）：`generator-output.schema.json`、`planner-output.schema.json`、
`scoring-output.schema.json`。Card 持久化 Schema 仍使用 v1。与生产校验器和 manifest 原子
验收并发布后即冻结；发布后的修正或演进走新 `vN/` 目录（规则见 `../../AGENTS.md`）。
