# AGENTS.md

Prompt 资产目录：`vN/` 下每文件一个 Prompt（planner / generator / rewrite）；scoring Prompt
与其 Rubric 同目录存放，但也由 `manifest.json` 的 `prompts.scoring` 入口加载。当前版本入口见
`../manifest.json` 的 prompts 节。

- 已发布 `vN/` 目录禁止原地修改；演进规则见 `../AGENTS.md`。
