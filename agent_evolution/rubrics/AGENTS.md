# AGENTS.md

Rubric 资产目录：`vN/` 下为评分标准（`rubric.md`）与打分 Prompt
（`scoring-prompt.md`）。评分标准入口为 `manifest.json` 的 `rubrics.main`，打分 Prompt 入口为
`prompts.scoring`。

- 已发布 `vN/` 目录禁止原地修改；演进规则见 `../AGENTS.md`。
