# AGENTS.md

仓库文档集合：需求（`PRD/`）、设计契约（`Architecture/`）、前端对接（`frontend/`）、工作流产物（`superpowers/`）、进度（`Progress.md`）。

- 需求权威 `PRD/V2.1/prd_v2_1.md`；设计契约单一事实来源规则见 `Architecture/AGENTS.md`。
- `Progress.md` 手动更新（日期 + 状态图例），不自动生成；改动主目录结构时同步其「文档清单」。
