# AGENTS.md

仓库文档集合：需求（`PRD/`）、设计契约（`Architecture/`）、前端对接（`frontend/`）、工作流产物（`superpowers/`）、进度（`Progress.md`）、交付报告（`final-delivery-report.md` / `r1-live-report.md`）。

- 需求权威 `PRD/V2.4/prd_v2_4.md`（继承 V2.3/V2.2/V2.1 历史需求，V2.4 增量为 email 登录键、username 展示名、会话滑动续期与前端错误提示 UX）；设计契约单一事实来源规则见 `Architecture/AGENTS.md`；前端对接见 `frontend/AGENTS.md`。
- `Progress.md` 手动更新（日期 + 状态图例），不自动生成；改动主目录结构时同步其「文档清单」。
- 交付报告按里程碑生成并保留（最终交付 / live 验证），属只读记录，不随代码演进反复改写。
